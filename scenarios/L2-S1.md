# L2-S1 handoff + ADAPT

cwd ralph = `notes-alpha` on `dev`. cwd same = `notes-beta` on `dev`.

Mechanical fixture calls `writeHandoffPackage(..., { port_mode: 'FULL' })`. Agent ports title persist by ADAPT into `useNotes.js`, not by copying `src/store/notes.js`. Omit episode `role`.
